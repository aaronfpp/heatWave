// App.swift
// heatWaveIOS
//
// Main entry point. Conforms to the SwiftUI App lifecycle (iOS 16+).
// No AppDelegate needed; SwiftUI manages the window and scene entirely.

import SwiftUI

@main
struct heatWaveIOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
